name: Tester
on: [push, pull_request]

jobs:
  PHPUnit:
    name: php-${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php:
          - 7.2
          - 7.3
    container:
      image: yansongda/php-fpm:${{ matrix.php }}
    steps:
      - uses: actions/checkout@master
      - name: Install Dependencies
        run: pwd && ls && composer update --no-interaction --no-suggest
      - name: Run PHPUnit
        run: pwd && ls && ./vendor/bin/phpunit
